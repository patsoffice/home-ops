machine:
  kubelet:
    extraMounts:
      - destination: /var/mnt/storage
        type: bind
        source: /var/mnt/storage
        options: ["bind", "rshared", "rw"]
  disks:
    - device: /dev/disk/by-id/nvme-CT1000P3SSD8_24484CA3E8D6
      partitions:
        - mountpoint: /var/mnt/storage
