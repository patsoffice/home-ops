{"id":"home-ops-hwq","title":"Theme local Authentik instance","status":"in_progress","priority":3,"issue_type":"task","created_at":"2026-03-31T15:07:43.906729Z","created_by":"pjl","updated_at":"2026-05-07T00:40:54.205912Z","source_repo":".","compaction_level":0,"original_size":0}
{"id":"home-ops-lxi","title":"Remove epicgames-freegames from gatus","status":"closed","priority":3,"issue_type":"chore","created_at":"2026-03-31T19:07:44.697430Z","created_by":"pjl","updated_at":"2026-04-02T02:58:30.138181Z","closed_at":"2026-04-02T02:58:30.137840Z","close_reason":"Added gatus.home-operations.com/enabled: false annotation to epicgames-freegames HTTPRoute to exclude it from gatus monitoring","source_repo":".","compaction_level":0,"original_size":0}
{"id":"home-ops-poe","title":"volsync: pin kopia cache hard limits to stop cache PVC overflow","description":"ROOT CAUSE\nvolsync-src-zwave-js-ui backup job failed: kopia could not write /cache/kopia.config\n(ENOSPC) — the 6Gi kopia cache PVC was full (~379k files). Ceph itself was healthy\n(11TiB free), so it was the dedicated cache PVC, not the backing pool.\n\nThe perfectra1n/volsync operator (internal/controller/mover/kopia/mover.go\ncalculateCacheLimits) auto-allocates kopia cache hard limits as metadata=70% +\ncontent=20% = 90% of cacheCapacity, \"leaving 10% for other files\". On a small cache\nPVC that 10% sliver cannot absorb kopia's /cache/logs, kopia.config, temp files, and\nbetween-sweep overshoot (kopia limits are soft, swept every few minutes; see\nkopia/kopia#4140), so the PVC fills and the backup fails. The mover image's entry.sh\nheader comment claims an auto-calc default but does NOT implement it (only applies a\nlimit when KOPIA_*_CACHE_SIZE_LIMIT_MB env is set); the real auto-calc lives in the\noperator at the too-aggressive 90%. Percentages are hardcoded constants, not\nconfigurable.\n\nFIX (done in working tree, not yet committed)\nPin kopia cache hard limits explicitly to content=20% / metadata=40% of cacheCapacity\n(60% total, ~40% headroom) via the CRD fields contentCacheSizeLimitMB /\nmetadataCacheSizeLimitMB.\n- components/volsync/replicationsource.yaml + replicationdestination.yaml: new\n  substitution vars VOLSYNC_KOPIA_CONTENT_CACHE_MB:=1024 / VOLSYNC_KOPIA_METADATA_CACHE_MB:=2048\n  (sized for the 5Gi default).\n- 25 app ks.yaml files: proportional per-app overrides next to VOLSYNC_CACHE_CAPACITY\n  (Flux postBuild can't do arithmetic, so values are precomputed). The four apps at\n  exactly 5Gi inherit the component default.\n\nFOLLOW-UPS\n- Recover zwave-js-ui: cache PVC was deleted and ReplicationSource paused during triage\n  — unpause (spec.paused=false) and confirm a clean backup once this is reconciled.\n- Commit + Flux reconcile; verify a backup on a small-cache app (zwave/zigbee2mqtt) and\n  a large one (plex) under the new limits.\n- Consider upstream report to perfectra1n/volsync: operator 90% allocation has too\n  little headroom for small caches, and the entry.sh \"auto-calc\" comment is stale/\n  misleading (not implemented in the script).","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-05-30T20:17:07.009154Z","created_by":"pjl","updated_at":"2026-05-30T20:40:15.833611Z","closed_at":"2026-05-30T20:40:15.833563Z","close_reason":"Fixed by pinning kopia cache hard limits (content=20%/metadata=40% of cacheCapacity, ~40% headroom) via contentCacheSizeLimitMB/metadataCacheSizeLimitMB, replacing the operator's too-tight 90% auto-calc. Shipped in two commits: canary (volsync component + zwave-js-ui + plex) then rollout to the remaining 23 apps. zwave-js-ui cache PVC was recreated and is backing up Successfully.","source_repo":"home-ops","source_repo_path":"/Users/pjl/ws/home-ops","compaction_level":0,"original_size":0}
{"id":"home-ops-ruk","title":"add RUBY_TCP_NO_FAST_FALLBACK to Manyfold","status":"closed","priority":1,"issue_type":"fix","created_at":"2026-03-22T19:06:05.418849Z","created_by":"pjl","updated_at":"2026-03-22T19:07:48.615498Z","closed_at":"2026-03-22T19:07:48.615194Z","close_reason":"disable Ruby 3.4 TCP fast fallback for manyfold","source_repo":".","compaction_level":0,"original_size":0}
{"id":"home-ops-tg1","title":"Add dragonflydb dependencies where missing","status":"closed","priority":1,"issue_type":"fix","created_at":"2026-03-22T19:38:33.018062Z","created_by":"pjl","updated_at":"2026-03-22T19:41:53.863171Z","closed_at":"2026-03-22T19:41:53.862777Z","close_reason":"add dragonfly-cluster dependency to apps using dragonfly","source_repo":".","compaction_level":0,"original_size":0}
{"id":"home-ops-volsync-source-namespace-null-td6","title":"Fix volsync component sourceNamespace null rendering (latent CRD-validation breakage)","description":"The reusable volsync component renders an invalid ReplicationDestination on any fresh apply, and will break every existing volsync-backed app the moment its ReplicationDestination is re-applied (ks change, CRD bump, or forced reconcile).\n\n## Root cause\n`kubernetes/components/volsync/replicationdestination.yaml:31` has:\n\n    sourceIdentity:\n      sourceName: ${APP}\n      sourceNamespace: ${VOLSYNC_SOURCE_NAMESPACE:=}\n\nNo app sets `VOLSYNC_SOURCE_NAMESPACE`, so it expands to empty, which YAML renders as `null`. The current volsync CRD rejects this:\n\n    ReplicationDestination \"<app>-dst\" is invalid: spec.kopia.sourceIdentity.sourceNamespace: Invalid value: \"null\": spec.kopia.sourceIdentity.sourceNamespace in body must be of type string\n\nExisting apps (e.g. convertx, applied 2026-01-18) have stored objects whose sourceIdentity contains only `sourceName` — the empty value was pruned under the older CRD — so they currently pass. They are NOT safe: a re-apply re-runs the dry-run and will fail the same way. tracefinity (the first new volsync app since the CRD tightened) hit this and is blocked.\n\n## Recommended fix\nPer the CRD schema, `sourceNamespace` is OPTIONAL: \"If not specified, defaults to the namespace of this ReplicationDestination.\" Only `sourceName` is required. Every app here has its ReplicationSource and ReplicationDestination in the same namespace, so the default is always correct.\n\nDrop the line entirely from the component:\n\n    sourceIdentity:\n      sourceName: ${APP}\n\nThis fixes all apps at once and matches the behavior already stored for existing objects. Then force-reconcile each volsync app (or let the next reconcile re-apply) to confirm the dry-run passes.\n\n## Stopgap already in place\n`kubernetes/apps/self-hosted/tracefinity/ks.yaml` sets `VOLSYNC_SOURCE_NAMESPACE: self-hosted` as a per-app workaround. Remove that override once the component is fixed.\n\n## Affected apps (29, all use components/volsync)\ndatabase/pgadmin; download/qbittorrent, sabnzbd; game/romm; home/esphome, home-assistant, zigbee2mqtt, zwave-js-ui; llm/ollama, open-webui; media/calibre-web-automated, lidarr, pinchflat, plex, radarr, sonarr, tautulli, tdarr; observability/gatus; self-hosted/convertx, filabridge, jrommanager, manyfold, paperless, spoolman, syncthing, tandoor, wordpress-gds, wordpress-poc.\n\n## Verification\nAfter the component fix + reconcile, for each app:\n  kubectl get replicationdestination <app>-dst -n <ns> -o jsonpath=\"{.status.conditions}\"\nand confirm no ReconciliationFailed events on the owning Kustomization.","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-05-22T23:23:03.245801580Z","created_by":"pjl","updated_at":"2026-05-22T23:33:12.062020332Z","closed_at":"2026-05-22T23:33:12.061970024Z","close_reason":"Fixed in component: removed the empty sourceNamespace line from kubernetes/components/volsync/replicationdestination.yaml (sourceNamespace is optional and defaults to the RD's own namespace, which is always correct here). Also removed the per-app VOLSYNC_SOURCE_NAMESPACE stopgap from tracefinity's ks.yaml. Fixes all 29 volsync apps at once. After deploy, force-reconcile a couple of existing apps to confirm dry-run passes.","source_repo":"home-ops","source_repo_path":"/home/pjl/ws/home-ops","compaction_level":0,"original_size":0,"labels":["core"]}
{"id":"home-ops-win","title":"Address tdarr-node OOM issues","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-03-31T15:07:14.320069Z","created_by":"pjl","updated_at":"2026-04-02T02:54:32.981921Z","closed_at":"2026-04-02T02:54:32.981558Z","close_reason":"Increased tdarr-node memory request from 256Mi to 512Mi and limit from 1Gi to 4Gi. The 1Gi limit was too low for GPU-accelerated video transcoding workloads.","source_repo":".","compaction_level":0,"original_size":0}
{"id":"home-ops-ypg","title":"Add Envoy Gateway outlier detection (BackendTrafficPolicy) to eject dead upstream endpoints","description":"On 2026-05-20, auth.chezlawrence.com (and any app fronted by Envoy Gateway) is vulnerable to a hard outage when an upstream's pod IPs change but the running envoy proxies hold a stale EDS snapshot. Root cause of that incident: after the envoy-gateway v1.7.3 -> v1.8.0 upgrade (commits 65fa5c6 + a070962), the envoy-external proxies last fully synced 05-17 ~20:35; authentik-server pods rolled 05-18 13:43 and got new IPs; the EDS delta never reached the running proxies, so Envoy kept dialing 3 dead IPs (10.192.0.214 / 10.192.2.204 / 10.192.3.27) with hundreds of cx_connect_fail each. Restarting envoy-gateway (control plane) did NOT help; only restarting envoy-external (data plane) cleared it.\n\nWhat turned a transient xDS glitch into a 100% outage: the route's cluster has NO active health check and NO outlier (passive) detection, so Envoy left the dead endpoints flagged health_flags::healthy and kept load-balancing onto them indefinitely. Outlier detection would have ejected the failing endpoints after N consecutive connect failures and let the request retry a (nonexistent-but-soon-correct) healthy host / fail fast instead of hanging 10s per request.","design":"Add an Envoy Gateway BackendTrafficPolicy enabling passive outlier detection (and optionally connection retry budget). Decide scope:\n- Cluster-wide: target the envoy-external (and envoy-internal) Gateway via targetRefs (kind: Gateway) so every backend inherits ejection. Simplest, broad protection.\n- Per-route: target individual HTTPRoutes (kind: HTTPRoute) for fine control.\n\nRecommended starting point (gateway-scoped) — tune thresholds:\n  spec.outlierDetection:\n    consecutive5XxErrors / consecutiveLocalOriginFailures (connect failures): ~5\n    interval: 10s\n    baseEjectionTime: 30s\n    maxEjectionPercent: 100 (so a fully-stale cluster can eject all and fail fast rather than hang)\n\nPlace under kubernetes/apps/network/envoy-gateway/ (e.g. a new gateway/ or app/ manifest) and wire into the kustomization. Verify the v1.8.0 CRD (BackendTrafficPolicy, gateway.envoyproxy.io/v1alpha1) supports the chosen fields. Note: no existing BackendTrafficPolicy in the repo yet.","acceptance_criteria":"1. A BackendTrafficPolicy with outlier detection is applied and Accepted (status conditions) on the envoy-external gateway (and envoy-internal if in scope).\n2. Simulated dead endpoint (e.g. scale a backend's pods so IPs change, or blackhole one) is ejected from the cluster within baseEjectionTime instead of receiving 100% of traffic — confirm via envoy admin /clusters (health_flags shows ejection) and absence of sustained cx_connect_fail growth.\n3. Document in the envoy-gateway README the operational note: roll envoy-external/envoy-internal proxy deployments after any envoy-gateway version upgrade to avoid stuck EDS.","status":"open","priority":2,"issue_type":"task","created_at":"2026-05-20T19:02:16.013558070Z","created_by":"pjl","updated_at":"2026-05-20T19:02:52.081144192Z","source_repo":".","compaction_level":0,"original_size":0}
